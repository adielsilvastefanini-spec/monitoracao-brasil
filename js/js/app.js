// Configuração e Inicialização Principal da Aplicação

// Função disparada assim que o DOM estiver totalmente carregado
document.addEventListener("DOMContentLoaded", function () {
  console.log("Aplicação principal (app.js) iniciada com sucesso.");
  
  // Inicialize aqui suas chamadas globais, eventos de botões ou Firebase
  fn_inicializarApp();
});

function fn_inicializarApp() {
  // Coloque aqui as funções gerais de inicialização que estavam no index.html
}
